#!/bin/bash
# ============================================================
#  兼容入口：转发到根目录统一启动脚本 start.sh
#  （原"启动控制面板"已合并进 start.sh，行为完全一致）
# ============================================================
exec bash "$(dirname "$0")/../start.sh"
